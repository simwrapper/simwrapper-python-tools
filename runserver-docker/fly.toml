# fly.toml app configuration file generated for vsp-cluster on 2023-12-13T15:48:24+01:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = "vsp-cluster"
primary_region = "ams"

[build]

[[mounts]]
  source = "simwrapper_data"
  destination = "/data"
  initial_size = "1gb"
  auto_extend_size_threshold = 0

[http_service]
  internal_port = 80
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 256
